# ai_bot

AI bot project workspace.

## Setup

1. Clone the repository.
2. Copy `.env.example` to `.env` and add your API keys.
3. Install dependencies (add instructions here once the stack is chosen).

## Development

Project structure and tooling will be added as the bot is built out.
